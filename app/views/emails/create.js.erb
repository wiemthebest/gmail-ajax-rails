emailElt = document.getElementById("emails");
emailElt.insertAdjacentHTML("afterbegin", "<%= j render 'email_in_list', email: @email %>");

$(".email-line").click(function(){
	let url = "emails/" + this.id
	$.ajax({
	    url: url,
	    type: "GET",
	    dataType : 'script', 
	});
})